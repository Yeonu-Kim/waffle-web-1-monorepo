{
  "name": "monorepo",
  "packageManager": "yarn@4.6.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check-all": "yarn workspaces foreach -A run check"
  },
  "devDependencies": {
    "eslint": "9.12.0",
    "typescript": "5.6.3"
  }
}
